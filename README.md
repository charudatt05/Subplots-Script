# Subplots-Script
This script gives subplots of basic diagnostic variables like precipitation (Simulated vs observed), Topography, Wind vectors, Cloud fraction (sim vs obs), Cloud albedo (sim vs obs), Time series of precipitation from WRF output file. 
# wrf_master script
This script automates the whole WPS and WRF processes and submits the job to HPC.
